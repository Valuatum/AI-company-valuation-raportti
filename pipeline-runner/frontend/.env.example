# Local dev: leave empty — Vite proxies /api → http://localhost:8000.
# On Vercel: set to the hosted backend origin (no trailing slash), e.g.
# VITE_API_BASE=https://valu-pipeline.fly.dev
VITE_API_BASE=
